import { FullDisplayNamePipe } from './full-display-name-pipe';

describe('FullDisplayNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FullDisplayNamePipe();
    expect(pipe).toBeTruthy();
  });
});
